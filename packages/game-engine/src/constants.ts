/**
 * Константы движка. Меняются только вместе с миграцией истории:
 * ставка, сыгранная при RTP 0.99, обязана переигрываться при RTP 0.99 и через год.
 * Поэтому конфиг игры пишется в саму ставку, а не читается из этого файла при разборе спора.
 */

/** Доля, возвращаемая игроку на бесконечной дистанции. 0.99 → эдж дома 1%. */
export const DEFAULT_RTP = 0.99;

/** Кап множителя. Ограничивает значение, а не отбрасывает раунд — иначе ломается распределение. */
export const DEFAULT_MAX_MULTIPLIER = 1_000_000;

/** Минимальный осмысленный таргет: при RTP < 1 множитель 1.00 всегда проигрышный. */
export const MIN_TARGET_MULTIPLIER = 1.01;

/** Скорость роста кривой crash: M(t) = e^(k·t). k = ln2/5 — удвоение за 5 секунд. */
export const CRASH_GROWTH_RATE = Math.LN2 / 5;
